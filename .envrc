source "$(nix build nixpkgs#nix-direnv --inputs-from . --print-out-paths --out-link .direnv/nix-direnv)/share/nix-direnv/direnvrc"

watch_file pyproject.toml uv.lock package.json package-lock.json
use flake .
